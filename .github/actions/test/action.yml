name: 'gem-test'
description: 'Test gem'
inputs:
  ruby-version:
    description: 'Testing Ruby version'
    required: true

runs:
  using: "composite"
  steps:
    - name: Install dependencies
      run: bundle install
      shell: sh
    - name: Run test
      run: rake
      shell: sh
