name: 'gem-test'
description: 'Test gem'

runs:
  using: "composite"
  steps:
    - name: Install dependencies
      run: bundle install
      shell: sh
    - name: Run test
      run: rake
      shell: sh
